class ArbitrarySuggestionType {
  //For the mock data type we will use review (perhaps this could represent a restaurant);
  String ticker, name, industry, sector;
  num marketcap;

  ArbitrarySuggestionType(
      this.ticker, this.name, this.marketcap, this.industry, this.sector);
}
