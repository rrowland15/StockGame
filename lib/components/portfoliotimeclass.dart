class PortfolioDataType {
  //For the mock data type we will use review (perhaps this could represent a restaurant);
  final DateTime timestamp;
  final int portstamp;

  PortfolioDataType(this.timestamp, this.portstamp);
}
